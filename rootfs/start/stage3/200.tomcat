# Set in stage2:
# ---------------------------------------------------------
# set -ef +am
# isFirstRun
# VAR_*
# All functions in /start/functions

if [ "$isFirstRun" == "true" ]
then
   if [ -n "$VAR_ROOT_APP" ] && [ -d "$VAR_ROOT_APP" ]
   then
      mv "$VAR_ROOT_APP" ROOT
         cd /
      fi
